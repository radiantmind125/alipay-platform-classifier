# SSP 检测线 · 测试结果与配置选项 · 2026-08-13

> 这份文档写的是**测出来的数字和几个可选配置**, 不是决定。
> 用哪一档、什么时候接、先只提示还是直接拒, **由经理定**。
>
> 上一版(2026-08-08)基于 `localdet3` 和 2 万张标定池, **数字全部作废**。
> 这一版换成 `localdet9` + 修好的金额定位器, 标定池 **98,025 张**。

## 一句话

三种手段叠加:
- **线路A** 抓"整张图是 AI 生成的";
- **线路B** 抓"AI 改了金额那一小块";
- **线路C** 读文件里 AI 服务**自己写进去的标记**, 不用模型, 近乎零成本。

三条互相独立, **命中任一条即可判**。

**实测两条模型线几乎完全互补**: 线路A 在局部合成上是 0.0%, 线路B 覆盖那里 71.9%;
反过来整图重绘归线路A。**多付的那 9 张误杀买的是一整类攻击, 不是几个百分点。**

---

## 标定池

`D:\probe\gen100k_blue` + `gen100k_white`, 共 97,975 张, 排除 4,323 张后 **98,025 张过线路A**,
其中 **96,011 张(97.9%)** 金额定位成功、能出线路B 信号。

排除名单 `exclude_big3.txt` 的构成(**没有一条依据模型分数**, 否则就是循环论证):

| 依据 | 来源 |
|---|---|
| 水印 / AIGC 元数据 | `watermark_scan` 认出的 38 张 |
| 画面实锤 | 人工逐张看过 |
| 造样本用过的源图 | 各批次 `manifest.csv` |
| **不是干净截图** | `screenshot_filter` 出的 1,277 张(相机 EXIF 435 / 短边 316 / 长宽比 526) |

> **`screenshot_filter` 那 1,277 张有一个前提**: 线上要真的把**翻拍**单独处理(拒掉或走人工)。
> 翻拍本来就是 SSP 最早的目标, 这个前提成立; 但**口径必须和线上一致**, 否则等于人为压低误杀率。
> 另外其中 842 张是按**尺寸/长宽比**剔的(平板截图、异形比例) —— 那是**业务范围**判断不是技术判断,
> 线上如果收平板截图, 这 842 张应该留在分母里。**这一条要经理点头。**

---

## 线路 A · 整图 AI 生成

**模型** `snapshot\aigen_v7\Net_epoch_best.pth`  **调用** `predict_all_models.py`  **用列** `final_ai_score`

真图最高分 **0.9996**。

| 误杀上限 | 分数线 | 万相整图 (n=50) | 千问整图 (n=299) | 千问整图 (n=150) | **局部合成 (n=547)** |
|---|---|---|---|---|---|
| 1/1000 | **0.6891** | 100.0% | 100.0% | 100.0% | **0.5 ~ 0.7%** |
| 1/5000 | **0.9440** | 54.0% | 92.3% | 96.0% | **0.0%** |
| 0(零误杀) | 0.9996 | 0.0% | 1.3% | 8.7% | 0.0% |

> **万相那格 n 只有 50**, 54.0% 的 95% 区间是 **40% ~ 67%** —— **不能当点估计对外报**。
> (对照: 洗池子前是 34.0%, 区间 22%~48%。确实变好了, 但精度撑不住一位小数。)
> 三格加权 89.6%, 但其中 **90% 的样本是千问**, 这个加权数基本等于千问, 别当成"整图平均水平"。

**线路A 在局部合成上是 0.0%** —— 这不是坏了, 是设计如此: 局部合成 99% 的像素还是原图,
整图检测器打近 0 分是正确行为。**所以局部改金额必须由线路B 兜, 两条线不可互相替代。**

## 线路 B · 局部改金额

**模型** `snapshot\localdet9\Net_epoch_best.pth`  **调用** `predict_tiled.py`  **用列** `tile_top3`

**参数(实测最优, 别改)**
```
--roi-amount --amount-pad 0 --blue-locator --agg top3 --roi-top 0.6
```

**出信号的前提: 金额区定位成功。** 定位不到就**不出信号** —— 是"无意见"不是"判真", 那些图只由线路A 判。
**修好定位器后能定位到的占 97.9%**(修之前白图只有 73%, 商户头像比金额高, 把数字整行滤掉了)。

真图最高分 **1.0000**。并集召回(线路A 或 线路B 任一命中即算抓到, 上线就是这么跑的):

| 格 | n | 硬拒 1/5000 | 复核 1/1000 |
|---|---|---|---|
| 万相蓝 | 195 | 94.9% | **100.0%** |
| 万相白 | 144 | 88.9% | 94.4% |
| 豆包白 | 150 | 81.3% | 93.3% |
| 豆包蓝 | 134 | 70.1% | 97.8% |
| 千问白 | 147 | **45.6%** | 88.4% |
| 千问蓝 | 142 | **42.3%** | 97.9% |
| **真实生成器加权** | **912** | **71.9%** | **95.5%** |

> 另有两格我们自己用 VAE 合成的(白 300 / 蓝 300, 硬拒 89.3% / 67.3%)。
> **不进对外口径** —— 模型训练时见过同一个生成器。
> (不过这两格**并不是**最高的, 合成蓝 67.3% 还低于万相蓝 94.9%; 说明记忆效应没在抬高数字。)

**千问是最弱的一格, 硬拒档两个版式都不到一半, 到复核档基本补回来。这是两档设计最强的论据。**

## 线路 C · AI 服务自报的标记(不用模型)

**调用** `watermark_scan.py` 的 `aigc_metadata()` —— 只读文件前 400KB 搜字符串, **不解码像素, 每张不到 1 毫秒**。

- **TC260 国标标签** `tc260.org.cn/ns/AIGC`。**国标要求国内 AI 服务都要打**, 所以**不挑厂商** ——
  等于免费覆盖了我们没训练过的生成器。
- **厂商串** `doubao` / `jimeng`, 常带生成任务号。

在 10 万池上扫出 **38 张**真假图。

> **边界: 骗子把标记删掉就抓不到。只能叠加在两条模型线上面, 不能替代。**
> `retouch`(醒图) / `meitu`(美图) 这类**修图 App 痕迹**只说明过了修图工具, **不是生成的铁证**,
> 单独报出来即可, **不要当假图处理**。

---

## ★ 两条模型线的阈值不能各标各的

**上线是两条一起跑, 任一条判拒就真拒了**, 所以真实误杀是**并集**:

```
拒绝 = (线路A分 >= thrA) 或 (线路B定位成功 且 线路B分 >= thrB)
```

| 各自预算 | 线路A 阈值 | 线路B 阈值 | A 命中 | B 命中 | 重合 | **并集** |
|---|---|---|---|---|---|---|
| 1/5000 | 0.9440 | 0.9883 | 19 | 19 | 10 | **28 = 1/3,501** |
| 1/1000 | 0.6891 | 0.7838 | 98 | 96 | 27 | **167 = 1/587** |

**对经理/风控报误杀, 必须报并集这个数。** 算它用 `combined_threshold.py --budget N`。

---

## 两档配置(建议)

| | 分数线 (A / B) | 并集误杀 | 整图覆盖 | 局部改金额覆盖 |
|---|---|---|---|---|
| **硬拒** | 0.9440 / 0.9883 | **1/3,501 = 2.9/万** | 千问 92~96%, 万相 54%(n=50) | **71.9%** |
| **人工复核** | 0.6891 / 0.7838 | **1/587 = 17.0/万** | **100%** | **95.5%** |

**复核档比硬拒档多送 139 张真图去人工(相当于 1/705), 换来局部改金额 +23.6 个覆盖点、整图补满到 100%。**

**为什么建议两档而不是一档**: 单用一条严线会把系统最强的部分一起废掉 ——
整图千问在复核档 100%、硬拒档 92%; 万相整图 100% -> 54%; 千问局部 97.9% -> 42.3%。
**一档式设计等于为了 2.9/万 的误杀, 放弃掉一半以上的千问覆盖。**

> **建议先只提示不拒绝跑一段时间**, 把"一万张 2.9 张"这个离线估计变成线上实测, 再谈要不要真的自动拒。
> **接不接、什么时候接, 由经理定。**

---

## ★ 提交文件的格式会显著影响覆盖率

标定池里 **49.6% 是 PNG, 50.0% 是 JPG**。而我们造的假图**全部是 JPEG q95**(两个生成器都写死了)。
补上 PNG 那一半之后测出来:

| 硬拒档 1/5000 | 存成 png | 存成 jpg |
|---|---|---|
| **源图是 png** | 94.3% | 92.3% |
| **源图是 jpg** | 91.3% | **76.0%** |

**单看任一因素都不贵(存 -2.0, 源 -3.0), 两个一起 -18.3。**
解释: **jpg 源铺了一层噪声底把痕迹盖住, jpg 存把痕迹本身削掉, 要两者兼备才藏得住。**

**线上只看得到用户提交的那个文件, 所以可以直接这么讲:**

> **提交上来是 PNG 的, 约 93% 抓得到; 是 JPEG 的, 约 84%。真实进件大约各占一半。**

进件侧记一下文件格式就能自己核对这个数。

**另: `.jpeg` 这个扩展名是个异常子群**(池里只占 0.4~0.5%):
中位数 0.2506 而 png/jpg 都是 0.0000; 在**两条线**的高分尾巴里分别超配 **27 倍**和 **41 倍**。
`.jpeg` 基本等于"这张图进过别的程序"。**把它单独路由到人工, 硬拒的线能从第 20 高降到约第 27 高。**

**顺带一个反直觉的结论: "另存为 PNG" 不是绕过路径, 反而更容易被抓。**
(反面解释也排除了: 若模型只是给无损图打高分, 48,632 张 PNG 真图就该被抬起来, 实测中位数 0.0000。)

---

## 覆盖边界(如实说清楚)

| 欺诈手法 | 谁抓 | 硬拒档 | 复核档 |
|---|---|---|---|
| 整张 AI 生成/重绘 | 线路A | 千问 92~96% / 万相 54%(n=50) | **100%** |
| **AI** 改金额那一小块 | 线路B | **71.9%** | **95.5%** |
| 翻拍(拿手机拍另一块屏) | 线路A | 早期实测 40 张 held-out 全中 | 同 |
| 带 AIGC 标记的(任何厂商) | 线路C | 标记还在就抓得到 | 同 |
| **手工**改金额(复制粘贴/重打字/加负号) | **不是我们** | **实测 0%** | **0%** |

## 已知短板

1. **手工改金额实测 0%, 且加训练也修不了。** 模型问的是"这块像素是不是神经网络生成的",
   而复制粘贴的像素本来就是真的。**要靠 OCR 读金额订单号去比对真实交易**(OCR 那条线的事)。
2. **千问是最弱的生成器**: 硬拒档局部只有 42~46%。复核档补到 88~98%。
3. **★ 金额定位器会在非收据页上误触发。** top30 里第 20 名(豆浆机商品页)和第 22 名(近乎全灰的空图)
   **都通过了"定位成功"**, 然后线路B 给那个垃圾裁块打了 0.99。
   **便宜的非循环修法: 在信任线路B 之前先对定位框做合理性校验**(位置/尺寸/里面是不是数字),
   而不是事后拿名单过滤。**这条还没做。**
4. **万相整图 n=50, 区间 40%~67%** —— 别报一位小数。豆包/万相真实服务的样本量一直偏小。
5. **重压缩同时打击两条线**(见上面 2x2: jpg 源 + jpg 存 = 76.0%)。**双重惩罚**:
   压缩把假图分数压下去, 同时把真图分数顶上来。(经理已知悉并表示暂不处理。)
6. **约 2.1% 的图定位不到金额** -> 没有局部信号, 只由线路A 判。覆盖面问题不是阈值问题。
7. **有真实但没训过的版式。** top30 第 29 名是**转出凭证**页 —— 是真的支付宝页面, 模型几乎肯定没见过。
8. **线路A 对无损 PNG 是分布外的。** 它的高分尾巴里 PNG 超配 1.25 倍(线路B 那边反过来, PNG 偏少 0.70 倍)。
   **把无损 PNG 真图加进线路A 的训练, 硬拒的线还能往下放。**
9. **豆包白那格的同源图没验证过。** `matrix_audit` 的空名单既可能是"验过没有"也可能是"没验成"。
   最坏情况(同源率按豆包蓝的 8.8% 算)该格掉约 1.7 点, **71.9% 的总数动约 0.3 点**。
10. **水印模板匹配有结构性盲区**: 水印叠在高对比 UI 元素上时抓不到。
    **所以"人工看榜首二三十张"这一步要一直保留。**

> **所有误杀数字都是上界。** 池子每洗一轮榜首都还能挖出东西 ——
> 这一轮洗掉 1,277 张翻拍和非收据, 覆盖率就从 59.8% 涨到 71.9%(**没重训, 没新数据, 没花钱**)。
> 不过**这一轮之后已经收敛**: top30 里翻拍清零, 只剩 2 张非收据, 再剔只值 0.0008 的阈值。

## 相关脚本

| 脚本 | 干什么 |
|---|---|
| `autoreject_threshold.py` | 单条线按误杀预算标阈值; `--exclude` 吃排除名单 |
| `combined_threshold.py` | **两条线的并集误杀**; `--budget N` 自己算阈值; `--fa/--fb` 算并集召回 |
| `union_matrix.py` | **八格 x 两档并集召回一次跑完**, 自动配对并剔同源 |
| `screenshot_filter.py` | 挑出翻拍/非收据/尺寸异常, 洗标定池(不看模型分数) |
| `top_score_sheet.py` | 真图榜首拼图, 供人工判读(唯一的非循环判据) |
| `watermark_scan.py` | 水印 + AIGC 元数据两条独立证据, 产出排除名单 |
| `reencode_jpeg.py` | 假图换编码重打分, 测重压缩绕过 |
| `gen_local_ai_edit.py` | 本地 VAE 造局部改金额样本; `--save-format` / `--src-ext` 控制压缩史 |
